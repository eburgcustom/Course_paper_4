# 📧 Сервис Email-рассылок

Django-приложение для управления email-рассылками с системой ролей и кешированием.

## 🚀 Возможности

### ✨ Основные функции
- 📧 Создание и управление email-рассылками
- 👥 Управление получателями и сообщениями
- 📊 Статистика отправок и отчеты
- ⏰ Планировщик рассылок по времени
- 🔄 Мгновенная отправка рассылок

### 🔐 Система ролей
- **Пользователь**: управляет только своими рассылками и клиентами
- **Менеджер**: просматривает все рассылки, может редактировать чужие данные
- **Администратор**: полное управление через Django Admin

### ⚡ Оптимизация
- 🚀 Серверное кеширование (Redis/LocMem)
- 📱 Клиентское кеширование CSS
- 📈 Ускорение загрузки до 650x

## 🛠️ Технологический стек

- **Backend**: Django 6.x, Python 3.13
- **Database**: PostgreSQL (можно SQLite для разработки)
- **Cache**: Redis/LocMem
- **Frontend**: Bootstrap 5
- **Email**: SMTP (Yandex/Gmail)
- **Package Manager**: Poetry
- **Environment**: python-decouple

## 📦 Установка и настройка

### 1. Клонирование репозитория
```bash
git clone <repository-url>
cd Course_paper_4
```

### 2. Установка зависимостей через Poetry
```bash
poetry install
poetry shell
```

### 3. Настройка переменных окружения
Создайте файл `.env`:
```env
SECRET_KEY=your-secret-key-here
DEBUG=True
EMAIL_HOST_USER=your-email@example.com
EMAIL_HOST_PASSWORD=your-app-password
DEFAULT_FROM_EMAIL=your-email@example.com
SERVER_EMAIL=your-email@example.com
PASSWORD_FOR_SUPER_USER=your-superuser-password
```

### 4. Миграции базы данных
```bash
python manage.py makemigrations
python manage.py migrate
```

### 5. Создание суперпользователя (кастомная команда)
```bash
python manage.py csu
```

### 6. Запуск сервера
```bash
python manage.py runserver
```

## 🏗️ Архитектура проекта

```
Course_paper_4/
├── config/                 # Настройки Django
│   ├── settings.py         # Основные настройки
│   ├── urls.py            # URL конфигурация
│   └── wsgi.py            # WSGI приложение
├── clients/               # Основное приложение
│   ├── models.py          # Модели (Mailing, Message, Recipient)
│   ├── views.py           # Представления
│   ├── forms.py           # Формы
│   ├── services.py        # Сервисный слой с кешированием
│   ├── mixins.py          # Миксины для прав доступа
│   ├── templates/clients/ # Шаблоны
│   └── static/            # Статические файлы
├── users/                 # Приложение пользователей
│   ├── models.py          # CustomUser с ролями
│   ├── views.py           # Аутентификация
│   ├── admin.py           # Админ-панель
│   ├── management/        # Кастомные команды
│   │   └── commands/
│   │       └── csu.py     # Создание суперпользователя
│   └── forms.py           # Формы регистрации
├── pyproject.toml         # Poetry конфигурация
├── poetry.lock            # Lock файл зависимостей
└── .env                   # Переменные окружения
```

## 👥 Роли и права доступа

### 🔹 Пользователь (User)
- ✅ Создание своих рассылок
- ✅ Управление своими получателями
- ✅ Просмотр своей статистики
- ❌ Не видит чужие данные

### 🔹 Менеджер (Manager)
- ✅ Просмотр всех рассылок
- ✅ Редактирование чужих рассылок
- ✅ Просмотр общей статистики
- ✅ Блокировка пользователей
- ✅ Отключение рассылок

### 🔹 Администратор (Admin)
- ✅ Полный доступ через Django Admin
- ✅ Управление ролями пользователей
- ✅ Системные настройки

## 🚀 Кеширование

### Серверное кеширование
- **Статистика**: 3 минуты
- **Списки рассылок**: 2 минуты
- **Автоматическая очистка** при изменении данных

### Клиентское кеширование
- **CSS файлы**: 5 минут
- **Meta-теги**: `Cache-Control: max-age=300`

### Проверка кеша
```bash
# Быстрая проверка
python -c "
from django.core.cache import cache
cache.set('test', 'OK', 60)
print('✅ Кеш работает' if cache.get('test') == 'OK' else '❌ Кеш не работает')
"
```

## 📧 Настройка email

### Yandex
```python
EMAIL_HOST = 'smtp.yandex.ru'
EMAIL_PORT = 465
EMAIL_USE_SSL = True
EMAIL_HOST_USER = 'your-email@yandex.ru'
EMAIL_HOST_PASSWORD = 'your-app-password'
```

### Gmail
```python
EMAIL_HOST = 'smtp.gmail.com'
EMAIL_PORT = 587
EMAIL_USE_TLS = True
EMAIL_HOST_USER = 'your-email@gmail.com'
EMAIL_HOST_PASSWORD = 'your-app-password'
```

## 🎯 Использование

### 1. Создание рассылки
1. Войдите в систему
2. Перейдите в "Рассылки" → "Создать рассылку"
3. Заполните форму (тема, сообщение, получатели)
4. Установите время отправки
5. Сохраните рассылку

### 2. Управление получателями
1. Перейдите в "Получатели"
2. Добавьте email получателей
3. Назначьте их рассылкам

### 3. Отправка рассылки
- **Автоматически**: по расписанию
- **Вручную**: кнопка "Отправить сейчас"

### 4. Просмотр статистики
- Главная страница показывает общую статистику
- Детальная статистика на странице рассылки


## 📝 Требования

### Python
- Python 3.8+

### Зависимости
```toml
[tool.poetry.dependencies]
python = "^3.13"
django = "^6.0"
python-decouple = "^3.8"
psycopg2-binary = "^2.9.11"
ipython = "^9.8.0"
pillow = "^12.0.0"
black = "^25.12.0"
redis = "^7.1.0"
```

### База данных
- PostgreSQL (рекомендуется)

## 👨‍💻 Автор

Разработано в рамках учебного проекта по курсу Django.

---

## 🎯 Быстрый старт

```bash
# 1. Клонирование
git clone <repository>
cd Course_paper_4

# 2. Установка зависимостей через Poetry
poetry install
poetry shell

# 3. Настройки
cp .env_example .env
# Отредактируйте .env файл (добавьте PASSWORD_FOR_SUPER_USER)

# 4. База данных
python manage.py migrate

# 5. Суперпользователь (кастомная команда)
python manage.py csu

# 6. Запуск
python manage.py runserver

# 7. Откройте http://127.0.0.1:8000
```

🚀 **Ваш сервис рассылок готов к работе!**
